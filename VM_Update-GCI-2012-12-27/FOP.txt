# Move downloaded binaries to /opt/fop-1.1
cd /opt
rm -r fop-1.0
mkdir fop-1.1
mv ~/Downloads/fop-1.1 ./fop-1.1
cd ~/Downloads
rm -r fop-*

# Update environment variables to reflect changes/new paths
sudo vi /etc/environment
:%s/fop-1.0/fop-1.1/g # Update all references to fop-1.0 to fop-1.1
